server:
    port: 8083
spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: jdbc:mysql://localhost:3306/logdb?useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul
        username: root
        password: root
    jpa:
        show-sql: true
        hibernate:
            ddl-auto: update
            format_sql: true